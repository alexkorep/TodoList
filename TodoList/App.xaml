<?xml version="1.0" encoding="utf-8" ?>
<Application xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="TodoList.App">
    <Application.Resources>
        <Color x:Key="DarkButtonBackground">DeepPink</Color>
        <Color x:Key="DarkButtonTextColor">White</Color> 
        <Style x:Key="FABPrimary" TargetType="Button">
            <Setter Property="CornerRadius" Value="100"/>
            <Setter Property="BackgroundColor" Value="{StaticResource DarkButtonBackground}"/>
            <Setter Property="HeightRequest" Value="55"/>
            <Setter Property="WidthRequest" Value="55"/>
            <Setter Property="HorizontalOptions" Value="CenterAndExpand"/>
            <Setter Property="VerticalOptions" Value="CenterAndExpand"/>
            <Setter Property="Padding" Value="2"/>
            <Setter Property="Margin" Value="0,0,15,15"/>
            <Setter Property="TextColor" Value="{StaticResource DarkButtonTextColor}"/>
            <Setter Property="FontSize" Value="32"/>
        </Style>
    </Application.Resources>
</Application>